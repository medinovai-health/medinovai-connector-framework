---
description: End session — summarize and hand off
---
Update `claude-progress.txt` with session summary, tests run, and next feature.
Ensure `feature_list.json` only toggles `passes` for completed items. Commit on a feature branch.
