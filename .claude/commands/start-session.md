---
description: Start a coding session — reads state and picks next feature
---
Run `bash init.sh`. Read `claude-progress.txt` and `feature_list.json`.
Run `git log --oneline -20`. Select the next feature with `"passes": false`.
State the feature id, plan, and how you will verify it (tests or curl).
