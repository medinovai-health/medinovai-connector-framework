---
description: Before destructive changes — create a git tag checkpoint
---
Create a tag `harness-checkpoint-pre-<operation>` with a short message.
Record the tag name in `claude-progress.txt`. After successful verification, delete the tag.
